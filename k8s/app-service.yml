apiVersion: v1
kind: Service
metadata:
  name: app-service
  namespace: sa-meter
spec:
  selector:
    app: meter-app
  type: NodePort
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30081
