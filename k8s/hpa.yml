apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: meter-app-hpa
  namespace: sa-meter
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: meter-app
  minReplicas: 2
  maxReplicas: 8
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 50
